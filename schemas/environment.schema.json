{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "exclave environment",
  "description": "One place the product runs. You have no network path to it; this file is everything the resolver knows.",
  "type": "object",
  "required": ["name", "tier", "channel", "kubernetes"],
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "tier": { "type": "string", "minLength": 1, "description": "Matched against a release's forbids.environmentTier." },
    "classification": { "type": "string", "description": "Matched against a release's allowedClassifications." },
    "channel": { "enum": ["stable", "candidate", "edge"] },
    "kubernetes": { "type": "string", "description": "Cluster version, e.g. \"1.29\"." },
    "schema": { "type": "integer", "minimum": 0 },
    "current": { "type": "string", "description": "Installed product version. Empty means nothing is installed, which leaves the upgrade path unconstrained." },
    "pinned": { "type": "string", "description": "Exact version, overriding channel tracking. Still subject to every other constraint." },
    "maintenanceWindow": { "type": "string", "description": "Reported, never scheduled. This is a resolver, not a cron system." }
  }
}
