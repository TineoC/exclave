apiVersion: v2
name: common
version: 0.1.0
type: library
description: Shared templates. Every service renders from these, so the shape of a
  Deployment or Service is defined once for the whole product.
